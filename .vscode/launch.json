{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "LLDB Debug",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/consumer",
      "cwd": "${workspaceFolder}/build",
      "stopOnEntry": false,
      "breakpointMode": "file",
      "preLaunchTask": "Build Debug"
    }
  ]
}
